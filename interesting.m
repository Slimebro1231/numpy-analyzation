function y = interesting(x)
    % This function calculates y = 2^x * log(x)
    % Input:
    %   x - The input value for which the function is calculated. x must be greater than 0.
    % Output:
    %   y - The calculated value of the function

    y = (2^x);
end
